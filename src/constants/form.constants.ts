export const EMAIL_INPUT = {
  minLength: 4,
  maxLength: 40,
  minLengthMessage: `Email must be at least 4 characters long!`,
  maxLengthMessage: `Email must be under 40 characters long!`
};

export const PASSWORD_INPUT = {
  minLength: 8,
  maxLength: 20,
  minLengthMessage: `Password must be at least 8 characters long!`,
  maxLengthMessage: `Password must be under 20 characters long!`
};

export const USERNAME_INPUT = {
  minLength: 1,
  maxLength: 20,
  minLengthMessage: `Username must be at least 1 characters long!`,
  maxLengthMessage: `Username must be under 20 characters long!`
};
